name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm
      - run: pnpm typecheck

  test-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm
      - run: pnpm test:unit

  test-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: durable_machines
          POSTGRES_PASSWORD: durable_machines
          POSTGRES_DB: durable_machines_test
        ports:
          - 5442:5432
        options: >-
          --health-cmd "pg_isready -U durable_machines"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      PG_TEST_DATABASE_URL: postgresql://durable_machines:durable_machines@localhost:5442/durable_machines_test
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm
      - run: pnpm test:integration

  lint-arch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm
      - run: pnpm lint:arch
