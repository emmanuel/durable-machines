services:
  postgres:
    image: postgres:17
    ports:
      - "5442:5432"
    environment:
      POSTGRES_USER: durable_xstate
      POSTGRES_PASSWORD: durable_xstate
      POSTGRES_DB: durable_xstate_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U durable_xstate -d durable_xstate_test"]
      interval: 2s
      timeout: 5s
      retries: 10
    tmpfs:
      - /var/lib/postgresql/data
